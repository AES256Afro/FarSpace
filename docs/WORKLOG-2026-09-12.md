# FarSpace work log, September 12, 2026

## Authorized work window

The user requested nine hours of work on all remaining milestones and added
quest location visibility as a priority. Start: 2026-09-12 02:31:03 UTC.
Deadline: 2026-09-12 11:31:03 UTC, or 06:31:03 America/Chicago.
At the deadline, stop starting implementation, finish a safe checkpoint,
record incomplete work and pause the continuation.

The existing task continuation was updated to FarSpace nine hour milestones,
using automation id farspace-six-hour-development. It runs every ten minutes.
There is no additional task or parallel agent work.

## Baseline and sequence

- Clean main at 684ed3cb0c18c93ecc4279559dc346fb4ce9a61b, v0.261.0.
- Previous release has 571 passing tests. Fresh checks accompany each slice.
- M428 through M432 are complete. M418 through M427 and M433 through M435
  remain open. Their dependencies and acceptance remain in NEXT-MILESTONES.md.
- First slice: quest destinations shared by galaxy, system, flight and orbit
  views. Follow actual quest stages, distinguish search areas from disclosed
  locations, and remove completed objectives without changing quest progress.
- Continue the documented menu, flight, contact, introductory voyage and recap
  work before autonomous mining and cargo logistics. Support, port, interior,
  research and full voyage work follow their recorded dependencies.
- Preserve real saves, identity and user tabs. Use isolated browser fixtures.
  Complete source checks, browser checks and publication verification separately.

## M436 / v0.262.0: Quest locations

- Added questLocations as a read-only destination projection. Accepted active
  contracts, current story and crew stages, service orders and council journeys
  provide their real destinations. Survey and discovery stages lead to planets
  or search areas; completed work leads to the issuing or delivery station.
- Both maps use diamonds and Q labels. Q toggles quest filtering without
  plotting or starting flight. Galaxy details put quest titles, locations and
  complete next actions first. System contacts put quest locations first and
  show the selected objective. Green diamonds indicate a ready contract.
- Explicit quest wrecks remain on the chart outside ordinary sensor range.
  Hidden anomalies keep their coordinates undisclosed until discovery. Future
  story locations and finished journeys do not appear prematurely.
- Flight uses the same exact local contacts. Orbit shows quest site markers,
  Q selects the exact site and holds its visible longitude until a manual
  selection, and E enters the selected site using the existing landing flow.
- Native browser checks verified pointer galaxy filtering, keyboard local
  filtering, required supply text and the correct payment station for a bounty.
  A first station check exposed orbital drift after stopping at the edge of
  interaction range. Changed the local stop distance from 80 to 40 metres.
  Production then stopped 33.291891 metres from the moving station with zero
  velocity, and native E began docking. Q selected ruin poi3-1 and E entered
  that same ruin. Fixture save writes stayed in memory; online calls were
  disabled. Browser warnings and errors: none.
- 587 tests in 37 files pass. TypeScript and production build pass. New checks
  cover stage changes, hidden contacts, return destinations, crew completion,
  filters, read-only projection, orbital selection and docking after drift.
  Save schema remains 16. Physical touch and gamepad checks were not performed.
- Published ba9be5364c43eb14b1204423bae4712919c4b135, tag v0.262.0. CI
  34668649526 passed, including the actual Cloudflare deployment step.
  Container run 34668649569 passed. A fresh public tab showed v0.262.0 with
  no warnings or errors. Health returned ok. Hosted assets match the build:
  JS index-DnR_t85b.js, SHA-256
  2a7e157d0d1e3487b892ec9a9eff616cca5a61b2f98e60ca5aa5e30922cfba12;
  CSS index-CwZdn5Vi.css, SHA-256
  4f9ba0bcb103d7e5d276c53d483e014ca4cc6c86a417f6d91da0474ff6e1e085.
- The final orbital fixture held rotation at 2.2734010118583137 radians after
  Q, with poi3-1 selected and its diamond visible. Manual selection releases
  the fixed view. Temporary tabs and preview5198 are closed. Dev5199 was
  restarted at v0.262.0. Bigbox and the catalog PR are unchanged.
- M418 source inventory is in MENU-INVENTORY.md. Roster has no viewport or
  pointer selection; Chronicle closes on any body click. Implement stable
  roster selection and scrolling first, then migrate suitable readers.
  The continuation remains active through the original nine hour deadline.

## M418 first slice: crew and records, v0.263.0

- Implemented stable selection and an eight row roster viewport. Full personal
  records have separate scrolling; row selection and domain actions have
  separate hit areas. Review return retains member, viewport and detail line.
  Crew on leave remain readable; bonus, review and course actions require an
  aboard member. Bonuses now save immediately after payment.
- Migrated Chronicle and all historical release notes to ReaderScene. Added
  complete text wrapping, explicit Back, paging, section navigation and search.
  External scene changes close pending search; flight return preserves its
  existing state. The local system map pages its actual seven visible rows.
- Browser testing caught the search input consuming the first Escape by
  clearing text. The dialog now handles that key directly and cancels while
  preserving the previous query. Unit and production browser checks passed.
- Native fixtures checked 20 aboard plus one on leave, an empty list, long
  names and records, page movement, wheel selection, a precise 100 credit
  bonus after inserting an earlier crew member, and review return. Chronicle
  End reached log entry 99. What's new End reached 0.10 and search found 0.261.
  Body clicks stayed open. Browser warnings/errors: none. Writes used memory
  storage; online calls were disabled. No real saves or identity changed.
- 603 tests in 40 files pass; TypeScript and production build pass. Schema 16
  unchanged. M418 remains open; MENU-INVENTORY.md records the remaining audits.
- Published a32ec7d5ae9bdbfe5cf00ecdc5241e19f31abb97, tag v0.263.0.
  CI 34670112694 passed, including the actual Cloudflare deployment step.
  Container workflow 34670112707 passed. A fresh public tab displayed v0.263.0
  without warnings or errors. Health returned ok. Hosted assets match:
  JS index-DbUZBtX6.js, SHA-256
  49f7ce1b45185c6fab2adcfe7df5964e60dac3fea2f313d3058443c35196d07f;
  CSS index-CwZdn5Vi.css, SHA-256
  4f9ba0bcb103d7e5d276c53d483e014ca4cc6c86a417f6d91da0474ff6e1e085.
- Production page controls moved the roster from rows 1-8 to 9-16; the wheel
  moved selection without transactions. Bonuses remained blocked for the
  selected leave entry. Returning from What's new restored its title button
  focus and kept the title scene unchanged. Empty roster controls stayed inert.
- Dev5199 restarted at v0.263.0. Temporary tabs and preview5198 are closed.
  Bigbox and its catalog PR are unchanged. Next: bounded orbital territories
  and sites, preserving Q quest focus and making E and L controls reachable.
  The original nine hour continuation remains active.

## M418 locations and objectives: v0.264.0

- Replaced unbounded orbital territory and site rows with separate six row
  lists. Fixed action buttons stay visible. Stable POI/Region selection and
  drawn pointer identities avoid shifted targets. Empty arrays remain valid.
  A disappearing target requires fresh input before landing or starting travel.
- Added nested full location/objective readers in orbit and the system map.
  I reads the selected destination; O reads all local objectives. Search and
  paging preserve the parent, selected target, camera and course. Hidden
  signals remain search areas. Q still reveals and holds the exact quest site.
- Letters now refresh without replacing the selected message or paragraph.
  New mail stays unread while another letter is displayed. Home/End and long
  sender wrapping make the complete message available.
- Native fixtures: 25 sites and 20 territories; Q selected the last site and
  pointer Land entered test-site-24. Pointer Rover entered territory 19.
  Twelve station quests were all readable, including the final requirement
  for 12 spare parts. Closing search/details preserved camera, course, target
  and world time. New mail retained Ari Sen at paragraph 18; End reached 129.
- Corrected a stale surface hint on return to orbit. Browser application
  warnings/errors: none. A test fixture string escape was corrected before
  running the letter checks. All state changes used memory storage and
  disabled online calls. Real saves and identity were preserved.
- 622 tests in 42 files pass. TypeScript passes. M418 remains open for the
  other station/location menus and final input/return audit.
- Production build passed. Published c99035a3756da1509c8fcb1258b984d07f3da7c7,
  tag v0.264.0. CI 34671740188 passed including actual Cloudflare deployment;
  container workflow 34671740324 passed. Public title showed v0.264.0 without
  errors. Health returned ok. Hosted assets match the build:
  JS index-sHWv4-dA.js, SHA-256
  7fcc2a5deea9fc02537ae02c1b0076a6aba54cfc4ff7ddc8f24c4ae53f18add6;
  CSS index-CwZdn5Vi.css, SHA-256
  4f9ba0bcb103d7e5d276c53d483e014ca4cc6c86a417f6d91da0474ff6e1e085.
- Production Q selected release-site-24 and pointer Land entered that ruin.
  Return to orbit retained the selected site and cleared the old surface hint.
  Temporary tabs and preview5198 are closed. Dev5199 restarted at v0.264.0.
  Bigbox and its catalog PR are unchanged. Continuation remains active.
- Next source findings: city and outpost panels still let hover overwrite
  keyboard selection, and accept clicks sharing a row Y even outside the
  panel X bounds. Reproduce and fix those first, then audit service/council
  actions and the remaining station tabs. Waystation has fixed walking
  interactions and at most three visitors; it has no scrolling list menu.

## 04:25 UTC, M418 settlement desks

- City markets, cantina crew offers, contracts and outpost goods use six row
  pages with arrows, wheel, Page Up/Down and Home/End. Pointer rows select;
  explicit Buy, Sell, Hire and Accept/Turn in controls perform transactions.
  Headers and margins are inert. Hover no longer changes keyboard selection.
- DeskMenu shares geometry and drawn row identity; domain actions stay in
  their scenes. Goods use commodity ids, offers use CrewMember objects and
  contracts use mission ids plus acceptance/hand-in state. Dynamic removal
  picks an adjacent item and requires fresh input before acting on it.
- I opens full details in ReaderOverlay. Closing retains the parent and list
  without replaying arrival effects. Desk input uses menu mode, then restores
  walking mode. City hiring counts reserved leave berths. Transactions update
  the existing ledger and autosave; outpost premiums and growth remain.
- Native acceptance: 20 goods at both desks, 15 crew offers including a long
  name and 15 long contracts. Exact last good buy/sell, last crew hire and
  contract acceptance passed. Full details reached requirement 39 plus the
  final contract marker. Wanted provisions paid 30CR and six growth; purchase
  was unavailable and spent nothing. Nested Escape returned to orbit.
- 646 tests in 43 files pass. TypeScript and production build pass. Native
  fixtures used memory storage and disabled online calls. No application
  errors. Actual saves and identity unchanged. Save schema stays 16.
- Published 6ebdc2482abc1c1256afa743c481d0af86b7b484, tag v0.265.0.
  CI 34673030263 passed, including Deploy to Cloudflare. Container workflow
  34673030030 passed. Hosted assets match the tested production build:
  JS index-CfFWItQP.js, SHA-256
  e75f8175a4afcd730a5d798fe6d1220e744dd57b46aa5986445c640ddc52d4ae;
  CSS index-CwZdn5Vi.css, SHA-256
  4f9ba0bcb103d7e5d276c53d483e014ca4cc6c86a417f6d91da0474ff6e1e085.
  Health returned ok. A fresh public tab showed v0.265.0. Its isolated city
  fixture selected Luxuries with End, bought one for 79CR, and returned from
  details with Luxuries still selected. No application warnings/errors.
  All temporary tabs are closed. Dev5199 restarted at v0.265.0. Bigbox and
  the catalog PR are unchanged. The nine hour continuation remains ACTIVE.
- M418 remains open. Next audit
  ServiceScene and CouncilScene, then remaining station tabs, galaxy removal,
  waystation and final input/caller returns. Include outpost foreman return:
  it enters the parent again after an encounter, unlike ReaderOverlay.
- ServiceScene actions need stable explicit ids, selected row retention and
  complete details. ServiceFileScene.open currently sets settingsReturn and
  enters a separate reader; its return calls ServiceScene.enter and resets
  cursor/scroll. Service fare encounters do the same. Preserve its actual
  order, fare and cutter rules while fixing return state. Council already
  has a world/station guarded resumeNext for agenda encounters; preserve it.
  Council action rows and response text are truncated without a full reader.

## 04:57 UTC, M418 office records and returns

- Service and council desks use six visible action rows with stable explicit
  ids, drawn row identity, arrows/wheel, paging and Home/End. Rows select;
  Enter/Do this executes. Removing a selected action requires fresh input
  before executing its adjacent replacement. Domain rules remain in scenes.
- I reads complete selected terms; O opens full office records and the latest
  reply. Service records include all retained reports and current orders.
  Council records include all minutes, the current sitting, mandate and reply.
  Readers preserve parent state and close pending search on scene changes.
- Service file and fare conversations retain action/viewport on return using
  a world/station guarded resume. Council retains its guarded chair return.
  File, fare and agenda callbacks preserve the latest reply for O.
- Foreman return preserves position and the selected good, refreshes newly
  constructed facilities and skips repeated clinic/chapel/oxygen effects.
  Plant repair follows that return state and still pays once. A queued plant
  repair cannot start after loading another world.
- Native service fixture: eight actions, return order and cutter loan; End
  and Page Down reached the depot action. Service archive End reached receipt
  49 of report 0; return retained history selection and viewport offset two.
  Reporting paid the displayed 950CR once. Declining a fare retained its offer.
  Council chair and cancelled agenda returned correctly; O read 20 minutes,
  the final paragraph and the last reply. Search found FINAL MINUTE 19.
  Escape returned to promenade 145,35. Two foreman conversations kept player
  position 165,35, morale 50 and oxygen eight. Waystation E and Escape kept
  all nine NPC objects and player position 3000,3000. Compare NPC objects,
  not the containing array, which AI filters each tick. No application errors.
- 667 tests in 44 files pass. The cutter pointer test was updated for explicit
  execution. Plant settlement and queued load cancellation are unit tested.
  Fixtures used memory storage and disabled online calls. No actual saves or
  identity changed. Save schema remains 16. Physical touch/gamepad stays M426.
- Production build passed. Published 9dc9aa70b062524cc9f166bb22e67234409c88d8,
  tag v0.266.0. CI 34674411003 passed including Deploy to Cloudflare.
  Container workflow 34674411280 passed. Hosted files match the tested build:
  JS index-D3K8FFat.js, SHA-256
  3e5856d77aa68568c85ddf31186963bd9dbea88bf7753cc5a22aa31b6483e298;
  CSS index-CwZdn5Vi.css, SHA-256
  4f9ba0bcb103d7e5d276c53d483e014ca4cc6c86a417f6d91da0474ff6e1e085.
  Health returned ok. A fresh public tab showed v0.266.0. In its isolated
  service fixture, I showed the full 750CR report terms and returned with the
  report selected. Enter paid 750CR once; O retained the complete reply.
  No application warnings/errors. All temporary tabs are closed. Dev5199
  restarted at v0.266.0. Bigbox and its catalog PR remain unchanged.
  The original nine hour continuation remains active. M418 remains open.
- Next: EncounterScene still sizes its dialog from all text/options without
  bounding to the screen. Long bodies/outcomes and many choices can overflow.
  Its click action checks row Y without checking X, and numeric selection can
  shift when requires filters change. Migrate those concrete cases while
  preserving its one-time outcome handling, domain callbacks and caller.
  Then audit remaining station tabs, galaxy selection/removal and final
  input/caller checks. Service, council, city, outpost and waystation acceptance
  is recorded in MENU-INVENTORY.md.

## 05:27 UTC checkpoint: v0.267.0, M418 conversations

- Conversation cards fit within the canvas. Eight story lines and four visible
  answers have separate paging. Outcomes show twenty lines with scroll and
  Home/End. I reads full answer terms; O reads the complete story and outcome.
  Nested readers retain selection and text position and clean pending search.
- Stable EncounterOption object identity survives conditional filtering and
  duplicate labels. Drawn row identity prevents stale pointer targeting. Rows
  select; Enter/Choose executes. Removed answers require fresh input before
  choosing a replacement. Both pointer axes are bounded; outcome text and
  margins are inert. Empty choices allow return; available choices still require
  an answer, preserving existing consequences.
- A resolution guard prevents reentrant callbacks. Parent results cannot replace
  a newly opened child conversation or reopen a caller after a scene/world
  change. SurfaceScene continues to consume rover damage once on return.
  The exported legacy wrap helper remains unchanged for other scenes.
- Native fixture: 13 answers, long label and terms, page navigation, final term
  and paragraph, exact answer 10 chosen once, forty paragraph outcome and
  empty flight return all passed. Flight kept the same NPC objects and player
  position. No application warnings/errors. Memory storage and disabled online
  calls protected real saves and identity. Physical touch/gamepad stays M426.
- 682 tests in 45 files pass. TypeScript and production build pass. Publication
  and hosted verification pending. Save schema remains 16. M418 remains open.
- Next: audit remaining StationScene tabs for stable selection, bounded text,
  input ownership and caller returns. Then complete galaxy selection/removal
  and the final M418 input audit. Continue M419 onward in dependency order.
