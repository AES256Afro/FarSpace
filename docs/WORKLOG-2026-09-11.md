# FarSpace work log, September 11, 2026

## Work window

Chris authorized six hours of continued development, from 12:34:39 UTC to
18:34:39 UTC (07:34:39 to 13:34:39 CDT). Stop starting implementation at the
deadline, finish a safe checkpoint, and pause the task continuation.

Continuation: `farspace-six-hour-development`, every ten minutes on this task.

## Baseline

- Local and remote `main`: `4aa3cff`, including the handoff.
- Version 0.232.0; milestones through 392.
- 153 tests passed; TypeScript and production build passed.
- Live JavaScript matched the local build; API health returned healthy.
- Production uses Cloudflare Workers, static assets, KV, and Durable Objects.
- The BoxPilot catalog currently points to 0.221.0.
- The 0.222.0 container workflow was cancelled. Container workflows for
  0.223.0 through 0.232.0 succeeded. No catalog PR was open at the start.

## Working order

1. Publish the successful pending catalog versions through one sequential
   chain, preserving the image verification and required PR checks.
2. Reconcile the fourth-sitting backlog with completed milestones 387-392.
3. Add visible ground companions and a two-person away team with useful
   exploration and support behavior, preserving old saves.
4. Continue with the singers' home or another feature whose dependencies
   are ready, leaving time for browser checks and release verification.

## Evidence and checkpoints

This section is updated as slices finish. Prior-session browser and frame-time
claims remain in the handoff; they are not fresh verification for this window.

### M393 / 0.233.0: two-person ground crews

- Added a paginated roster for up to two fit crew, reachable at touchdown
  and with T at the lander. Includes keyboard and pointer controls.
- Crew appear on nearby reachable ground at stops and shelter in the rover
  during movement or storms. Numbered suit markers match the roster.
- Distinct role effects combine; duplicate roles do not multiply a bonus.
- Corrected the science scan amount shown to the player and added tangible
  pilot, medic, and lookout benefits. Return rewards apply once per landing.
- Reconciled completed items in the fourth-sitting backlog. Updated the
  player guide, controls, in-game release notes, and local catalog manifest.
- Validation: 161 tests passed. TypeScript and production build passed;
  the existing bundle-size advisory remains (1,097.74 kB before gzip).
- Local browser: keyboard and mouse roster selection, lander reassignment,
  visible suits, a 60-point engineering repair for one spare, and a scan
  awarding and announcing 150 data. The scan used an in-memory held-input
  fixture; no physical gamepad or touch device was tested. No console errors.
- Native E at the lander returned to orbit: one completed trip, both chosen
  crew at morale 73 and loyalty 0.3, the third crew member unchanged, and the
  transient team cleared. The temporary browser fixture was closed.
- Released as `94a38712541f2b640d84035f2f5dce18595c6aee`, tagged `v0.233.0`.
  Remote main and tag were verified. Hosted CI/deploy run `34601094170`
  succeeded; container image run `34601094230` succeeded.
- Production asset `index-C0fNrlwe.js` matched the local production build:
  SHA-256 `bad758ca35cc6a67143b5191cbf29e2e21eb5e38ee9ef0ee61f46aabc37b4cd4`.
  `/api/health` returned HTTP 200 with `ok: true`.

### Catalog queue

- The original guarded queue finished at 13:59 UTC, with log
  `/private/tmp/farspace-six-hour-20260911/catalog.log` and current PR state in
  `catalog-current.json` beside it. Inspect before starting another chain.
- Script: `/private/tmp/farspace-six-hour-20260911/catalog-queue.mjs`.
  Each version uses a fresh isolated BoxPilot clone and explicit repository
  arguments; it requires passing `validate` and `tags-resolve` checks before
  merging the exact reviewed commit. Failed checks get one retry.
- 0.222.0 skipped because its image run was cancelled.
- Versions 0.223.0 through 0.232.0 merged in BoxPilot PRs #229-#238.
- Version 0.235.0 merged in PR #239 at 14:18 UTC, after both required checks
  passed. Merge commit: `c442addc49943770347092dbe52c6af5a4713666`.
- Version 0.236.0 merged in PR #240 at 14:53 UTC, commit
  `0545bffb9c9b61de29d203a0fbfd6e5c9e0dab37`. The first validation run failed
  on an unrelated existing AppCatalog owner-password UI test; its one retry
  passed. No BoxPilot application code changed.
- The script now takes explicit version arguments. Publish the next successful
  release only after inspecting current PR state and confirming no open chain.

### M394 / 0.234.0: the singers' returning room

- Added a visible dockable berth at the known singers' home after either
  roll-call response. Admission uses persistent contact progress, including
  old saves without the keepsake. No human faction table changes.
- G then R plots the berth, with a pointer button on the galaxy map. N flies
  through gates and to the berth on arrival. E docks inside 160 below speed 45.
- Exchange 100 exploration data for 5 light. Buy fuel, hull repairs, parts,
  and relics; partial service quotes its actual amount and whole-light cost.
  Insufficient funds or space consume nothing. The account persists.
- Returning from the berth and galaxy map preserves flight contacts.
- Validation: 168 tests pass; TypeScript and production build pass. The
  existing bundle-size advisory remains, at 1,105.38 kB before gzip.
- Native browser: E docking, keyboard survey trades, mouse parts purchase,
  hull repair, insufficient-light rejection, G/R routing, and N approach
  from 700 units. Autopilot stopped at 107.12 units with zero speed, then E
  docked. All 11 original NPC objects survived the menu returns. No runtime
  errors. Temporary unlinked in-memory fixtures used disabled autosaves and
  were closed; no physical touch device or gamepad was tested.
- Released as `f03ded8a81a938b608ff3f949b75798f39178922`, tagged `v0.234.0`.
  Remote main and tag verified. Hosted CI/deploy run `34603740323` and
  container image run `34603740437` succeeded. The Cloudflare deploy step
  ran successfully rather than taking the missing-secret skip path.
- Production `/assets/index-g39lyDL8.js` matches the local build, SHA-256
  `327a9a140b0adc21efb60ee1b19466861feca1979b775abac98888a85d9f20b2`.
  `/api/health` returned `ok: true`. TLS validation remained enabled;
  use system curl because this Python installation lacks its CA bundle.

### M395 / 0.235.0: a cabin for the listening bowl

- Research station lounges away from the known singers' home offer a fare
  after contact. The passenger occupies one cabin and pays 20 light plus
  5 per route hop, fixed at booking. Accepting plots the berth route.
- No docking-count deadline. A separate passenger kind keeps homecoming
  payment, human station hand-in, and generic returning fares separate.
  Boarding rejects duplicate and stale offers; arrival pays once.
- Added the traveler aboard, lounge dialogue, crew chat, captain's-table
  story, ship comments, a guestbook entry, and the word return on homecoming.
- Crowded bars scroll to keep the selected row visible with aligned mouse
  targets. A stationary pointer no longer cancels keyboard or wheel movement.
- Handbook, player guide, release notes, README and roadmap updated. M394 is
  now beside M393; removed the duplicate M187 heading at the end of the list.
- Validation: 177 tests pass; TypeScript and production build pass. Existing
  bundle-size advisory: 1,112.19 kB before gzip, 392.24 kB gzipped.
- Native browser: 16 Down presses through a bar with seven patrons and nine
  recruits reached the singer fare; a mouse click boarded them. Up worked
  with the pointer stationary. Talked to the passenger aboard, used E for
  the homeward gate, and confirmed the final target remained the berth.
  E at the berth paid 25 light, freed the cabin, signed the guestbook, and
  taught return. Repeat docking kept one payment and one guestbook entry.
- World and approach positions were in-memory fixtures; autosaves disabled,
  browser unlinked, fixture tab closed. No console errors. No physical touch
  or gamepad tested. Tests cover long trips and save migration separately.
- Released as `6e5519d5d9e6333488b208612f2db9844043ed7e`, tagged `v0.235.0`.
  Remote main and tag verified. Hosted CI/deploy `34606240516` and container
  image `34606240276` succeeded. The Cloudflare deploy step ran successfully.
- Production `/assets/index-wySVnwi1.js` matches the local build, SHA-256
  `8f79fa1131b2249c34094cf689334224e15f6a08bc0f12f203abcc237d5941a5`.
  `/api/health` returned `ok: true`.

### M396 / 0.236.0: the ships you left outside

- Ship interior, orbit and wreck returns preserve the active flight population,
  including rescue progress, escorts, tow lines, loose cargo and projectiles.
- Return requests require the same World object and system, with no real
  station departure. Jumps clear local ship and combat references and record
  the new system, while keeping the plotted flight controls.
- Delayed wire and encounter callbacks check their original population before
  acting. F9 stops the old update frame immediately after loading.
- Validation: all 190 tests pass; TypeScript and production build pass.
  Existing bundle-size advisory: 1,114.11 kB before gzip, 392.80 kB gzipped.
- Native browser: I to enter the ship, E at the cockpit and Escape to leave,
  E into orbit and Escape back, E to board a wreck and Escape back. The same
  NPC objects, rescue job, escort and tow survived. A naturally occurring
  parley card also returned to those contacts.
- Native E at a gate changed sys0 to sys5, removed every old NPC reference,
  cleared rescue/tow state, and retained the destination's new NPCs through
  another I/Escape ship visit. No runtime errors.
- Positions and rescue setup used temporary in-memory fixtures with autosaves
  disabled and no callsign. The fixture tab was closed. No physical touch
  device or gamepad was tested.

- Released as `949e2a91146b26a77756a665630d07b85340a305`, tagged `v0.236.0`.
  Remote main and tag verified. Hosted CI/deploy `34609726525` succeeded,
  including the actual Cloudflare deploy step. Container `34609720905`
  passed on attempt 2. The first attempt stalled after QEMU crashed with an
  illegal instruction during ARM npm installation, and was cancelled.
- Production `/assets/index-CA3D2299.js` matches the local build, SHA-256
  `be90cb6fd8d82183824c4678f015161aa44c5078d6ebbeb6cb5e00a5b1aa6575`.
  `/api/health` returned `ok: true`.

### M397 / 0.237.0: the last entry

- An old hand can choose a reachable final port, preferring home. They remain
  on the watch bill until the captain says goodbye beside them on its deck.
  There is no time or docking limit. Existing immediate retirement remains.
- The roster and shipboard conversation show the promise. N from the roster
  restores its course; flight follows the gates and then the exact station.
  New map courses and singer bookings replace that station target explicitly.
- The deck gathering supports a free farewell, an optional 300 credits, or
  another minute together. Settlement removes the crew member once, records
  an alumnus, gives the ship a watch tally, and queues one later letter.
  Existing shore leave and departures retain their normal ownership rules.
- News now opens complete letters with L or a preview click. Older/newer
  navigation, scrolling, and read state work for existing and new letters.
  Retired crew can be addressed even when standing beside a service kiosk.
- Docker's static-asset build now uses BUILDPLATFORM. The final nginx images
  remain amd64 and arm64. Removed unnecessary QEMU setup after the observed
  0.236.0 crash. Local Docker is unavailable; hosted image validation remains
  part of this release's verification.
- Validation: all 206 tests pass; TypeScript, production build and diff checks
  pass. Existing size advisory: 1,125.34 kB raw, 397.14 kB gzipped.
- Native browser: accepted the last journey, restored the roster course,
  spoke aboard, used a gate, docked at the chosen port, opened the deck
  farewell, deferred it, and paid the optional bonus. One alumnus, keepsake,
  payment and queued letter were recorded. Opened the delivered letter,
  navigated to older mail, scrolled, closed, and opened a preview by mouse.
  A later visit displayed the retired crew member's new dialogue.
- Destination approach and world state used temporary in-memory fixtures.
  The full route was not manually flown. Deck walking used held-input fixture
  state; interactions used native keys and pointer clicks. Autosaves were
  disabled and the browser unlinked. No application console errors; temporary
  tabs closed. No physical touch device or gamepad was tested.
- Source publication, hosted deployment, image and catalog are pending at
  this checkpoint.

### Next slice

M393-M396 are released and verified. M397 is ready for release at 14:59 UTC.
After verifying that release, continue with the belt council as a physical
place, the service career, or the ship's own sim program. Inspect the existing
belt votes, freeman standing, and civic correspondence before extending them.

Flight population ownership and reset boundaries live in FlightScene.enter
and resetPopulation. Keep direct jump entry in doJump synchronized with this
context; it repopulates without calling enter. The regression suite is in
tests/flight-continuity.test.ts.

The last journey lives in src/core/lastleg.ts, StationScene.retirement,
StationWalkScene.farewell, and tests/lastleg.test.ts. LettersScene and its tests
cover reading and return context. Optional fields persist at save version 14.

Passenger implementation lives in `src/core/singers.ts`; it uses the existing
Mission passenger list and an optional light reward. `genFares` excludes
singer guestbook entries from generic returning fares. `StationScene` handles
boarding, scrolls its bar, and rejects human hand-in of a singer. Deliveries
are settled in `SingersScene.enter`. `tests/singer-passenger.test.ts` covers
these boundaries. The in-game handbook content is `src/data/almanac.ts`.

The local Vite server is running at `http://127.0.0.1:5199` in exec session
17853. Its version define is cached until restart; production builds read
package.json correctly. Test using temporary in-memory worlds and no callsign
or cloud link, and disable fixture autosaves to preserve local saves.
The release tag helper remains at the scratchpad path in the original
handoff; inspect it before using it, and use the explicit FarSpace directory.
