# FarSpace work log, September 11, 2026

## Work window

Chris authorized six hours of continued development, from 12:34:39 UTC to
18:34:39 UTC (07:34:39 to 13:34:39 CDT). Stop starting implementation at the
deadline, finish a safe checkpoint, and pause the task continuation.

Continuation: `farspace-six-hour-development`, every ten minutes on this task.

## Baseline

- Local and remote `main`: `4aa3cff`, including the handoff.
- Version 0.232.0; milestones through 392.
- 153 tests passed; TypeScript and production build passed.
- Live JavaScript matched the local build; API health returned healthy.
- Production uses Cloudflare Workers, static assets, KV, and Durable Objects.
- The BoxPilot catalog currently points to 0.221.0.
- The 0.222.0 container workflow was cancelled. Container workflows for
  0.223.0 through 0.232.0 succeeded. No catalog PR was open at the start.

## Working order

1. Publish the successful pending catalog versions through one sequential
   chain, preserving the image verification and required PR checks.
2. Reconcile the fourth-sitting backlog with completed milestones 387-392.
3. Add visible ground companions and a two-person away team with useful
   exploration and support behavior, preserving old saves.
4. Continue with the singers' home or another feature whose dependencies
   are ready, leaving time for browser checks and release verification.

## Evidence and checkpoints

This section is updated as slices finish. Prior-session browser and frame-time
claims remain in the handoff; they are not fresh verification for this window.

### M393 / 0.233.0: two-person ground crews

- Added a paginated roster for up to two fit crew, reachable at touchdown
  and with T at the lander. Includes keyboard and pointer controls.
- Crew appear on nearby reachable ground at stops and shelter in the rover
  during movement or storms. Numbered suit markers match the roster.
- Distinct role effects combine; duplicate roles do not multiply a bonus.
- Corrected the science scan amount shown to the player and added tangible
  pilot, medic, and lookout benefits. Return rewards apply once per landing.
- Reconciled completed items in the fourth-sitting backlog. Updated the
  player guide, controls, in-game release notes, and local catalog manifest.
- Validation: 161 tests passed. TypeScript and production build passed;
  the existing bundle-size advisory remains (1,097.74 kB before gzip).
- Local browser: keyboard and mouse roster selection, lander reassignment,
  visible suits, a 60-point engineering repair for one spare, and a scan
  awarding and announcing 150 data. The scan used an in-memory held-input
  fixture; no physical gamepad or touch device was tested. No console errors.
- Native E at the lander returned to orbit: one completed trip, both chosen
  crew at morale 73 and loyalty 0.3, the third crew member unchanged, and the
  transient team cleared. The temporary browser fixture was closed.
- Released as `94a38712541f2b640d84035f2f5dce18595c6aee`, tagged `v0.233.0`.
  Remote main and tag were verified. Hosted CI/deploy run `34601094170`
  succeeded; container image run `34601094230` succeeded.
- Production asset `index-C0fNrlwe.js` matched the local production build:
  SHA-256 `bad758ca35cc6a67143b5191cbf29e2e21eb5e38ee9ef0ee61f46aabc37b4cd4`.
  `/api/health` returned HTTP 200 with `ok: true`.

### Catalog queue

- The original guarded queue finished at 13:59 UTC, with log
  `/private/tmp/farspace-six-hour-20260911/catalog.log` and current PR state in
  `catalog-current.json` beside it. Inspect before starting another chain.
- Script: `/private/tmp/farspace-six-hour-20260911/catalog-queue.mjs`.
  Each version uses a fresh isolated BoxPilot clone and explicit repository
  arguments; it requires passing `validate` and `tags-resolve` checks before
  merging the exact reviewed commit. Failed checks get one retry.
- 0.222.0 skipped because its image run was cancelled.
- Versions 0.223.0 through 0.232.0 merged in BoxPilot PRs #229-#238.
- Version 0.235.0 merged in PR #239 at 14:18 UTC, after both required checks
  passed. Merge commit: `c442addc49943770347092dbe52c6af5a4713666`.
- Version 0.236.0 merged in PR #240 at 14:53 UTC, commit
  `0545bffb9c9b61de29d203a0fbfd6e5c9e0dab37`. The first validation run failed
  on an unrelated existing AppCatalog owner-password UI test; its one retry
  passed. No BoxPilot application code changed.
- Version 0.237.0 merged in PR #241 at 15:03 UTC, commit
  `371776a1cbe51459221000011d5c6aac9872a4e5`. Both required checks passed.
- Version 0.238.0 merged in PR #242 at 15:24 UTC, commit
  `c394439ea2437e38b800bcc53997398d8d5a923d`. Both required checks passed.
- The script now takes explicit version arguments. Publish the next successful
  release only after inspecting current PR state and confirming no open chain.

### M394 / 0.234.0: the singers' returning room

- Added a visible dockable berth at the known singers' home after either
  roll-call response. Admission uses persistent contact progress, including
  old saves without the keepsake. No human faction table changes.
- G then R plots the berth, with a pointer button on the galaxy map. N flies
  through gates and to the berth on arrival. E docks inside 160 below speed 45.
- Exchange 100 exploration data for 5 light. Buy fuel, hull repairs, parts,
  and relics; partial service quotes its actual amount and whole-light cost.
  Insufficient funds or space consume nothing. The account persists.
- Returning from the berth and galaxy map preserves flight contacts.
- Validation: 168 tests pass; TypeScript and production build pass. The
  existing bundle-size advisory remains, at 1,105.38 kB before gzip.
- Native browser: E docking, keyboard survey trades, mouse parts purchase,
  hull repair, insufficient-light rejection, G/R routing, and N approach
  from 700 units. Autopilot stopped at 107.12 units with zero speed, then E
  docked. All 11 original NPC objects survived the menu returns. No runtime
  errors. Temporary unlinked in-memory fixtures used disabled autosaves and
  were closed; no physical touch device or gamepad was tested.
- Released as `f03ded8a81a938b608ff3f949b75798f39178922`, tagged `v0.234.0`.
  Remote main and tag verified. Hosted CI/deploy run `34603740323` and
  container image run `34603740437` succeeded. The Cloudflare deploy step
  ran successfully rather than taking the missing-secret skip path.
- Production `/assets/index-g39lyDL8.js` matches the local build, SHA-256
  `327a9a140b0adc21efb60ee1b19466861feca1979b775abac98888a85d9f20b2`.
  `/api/health` returned `ok: true`. TLS validation remained enabled;
  use system curl because this Python installation lacks its CA bundle.

### M395 / 0.235.0: a cabin for the listening bowl

- Research station lounges away from the known singers' home offer a fare
  after contact. The passenger occupies one cabin and pays 20 light plus
  5 per route hop, fixed at booking. Accepting plots the berth route.
- No docking-count deadline. A separate passenger kind keeps homecoming
  payment, human station hand-in, and generic returning fares separate.
  Boarding rejects duplicate and stale offers; arrival pays once.
- Added the traveler aboard, lounge dialogue, crew chat, captain's-table
  story, ship comments, a guestbook entry, and the word return on homecoming.
- Crowded bars scroll to keep the selected row visible with aligned mouse
  targets. A stationary pointer no longer cancels keyboard or wheel movement.
- Handbook, player guide, release notes, README and roadmap updated. M394 is
  now beside M393; removed the duplicate M187 heading at the end of the list.
- Validation: 177 tests pass; TypeScript and production build pass. Existing
  bundle-size advisory: 1,112.19 kB before gzip, 392.24 kB gzipped.
- Native browser: 16 Down presses through a bar with seven patrons and nine
  recruits reached the singer fare; a mouse click boarded them. Up worked
  with the pointer stationary. Talked to the passenger aboard, used E for
  the homeward gate, and confirmed the final target remained the berth.
  E at the berth paid 25 light, freed the cabin, signed the guestbook, and
  taught return. Repeat docking kept one payment and one guestbook entry.
- World and approach positions were in-memory fixtures; autosaves disabled,
  browser unlinked, fixture tab closed. No console errors. No physical touch
  or gamepad tested. Tests cover long trips and save migration separately.
- Released as `6e5519d5d9e6333488b208612f2db9844043ed7e`, tagged `v0.235.0`.
  Remote main and tag verified. Hosted CI/deploy `34606240516` and container
  image `34606240276` succeeded. The Cloudflare deploy step ran successfully.
- Production `/assets/index-wySVnwi1.js` matches the local build, SHA-256
  `8f79fa1131b2249c34094cf689334224e15f6a08bc0f12f203abcc237d5941a5`.
  `/api/health` returned `ok: true`.

### M396 / 0.236.0: the ships you left outside

- Ship interior, orbit and wreck returns preserve the active flight population,
  including rescue progress, escorts, tow lines, loose cargo and projectiles.
- Return requests require the same World object and system, with no real
  station departure. Jumps clear local ship and combat references and record
  the new system, while keeping the plotted flight controls.
- Delayed wire and encounter callbacks check their original population before
  acting. F9 stops the old update frame immediately after loading.
- Validation: all 190 tests pass; TypeScript and production build pass.
  Existing bundle-size advisory: 1,114.11 kB before gzip, 392.80 kB gzipped.
- Native browser: I to enter the ship, E at the cockpit and Escape to leave,
  E into orbit and Escape back, E to board a wreck and Escape back. The same
  NPC objects, rescue job, escort and tow survived. A naturally occurring
  parley card also returned to those contacts.
- Native E at a gate changed sys0 to sys5, removed every old NPC reference,
  cleared rescue/tow state, and retained the destination's new NPCs through
  another I/Escape ship visit. No runtime errors.
- Positions and rescue setup used temporary in-memory fixtures with autosaves
  disabled and no callsign. The fixture tab was closed. No physical touch
  device or gamepad was tested.

- Released as `949e2a91146b26a77756a665630d07b85340a305`, tagged `v0.236.0`.
  Remote main and tag verified. Hosted CI/deploy `34609726525` succeeded,
  including the actual Cloudflare deploy step. Container `34609720905`
  passed on attempt 2. The first attempt stalled after QEMU crashed with an
  illegal instruction during ARM npm installation, and was cancelled.
- Production `/assets/index-CA3D2299.js` matches the local build, SHA-256
  `be90cb6fd8d82183824c4678f015161aa44c5078d6ebbeb6cb5e00a5b1aa6575`.
  `/api/health` returned `ok: true`.

### M397 / 0.237.0: the last entry

- An old hand can choose a reachable final port, preferring home. They remain
  on the watch bill until the captain says goodbye beside them on its deck.
  There is no time or docking limit. Existing immediate retirement remains.
- The roster and shipboard conversation show the promise. N from the roster
  restores its course; flight follows the gates and then the exact station.
  New map courses and singer bookings replace that station target explicitly.
- The deck gathering supports a free farewell, an optional 300 credits, or
  another minute together. Settlement removes the crew member once, records
  an alumnus, gives the ship a watch tally, and queues one later letter.
  Existing shore leave and departures retain their normal ownership rules.
- News now opens complete letters with L or a preview click. Older/newer
  navigation, scrolling, and read state work for existing and new letters.
  Retired crew can be addressed even when standing beside a service kiosk.
- Docker's static-asset build now uses BUILDPLATFORM. The final nginx images
  remain amd64 and arm64. Removed unnecessary QEMU setup after the observed
  0.236.0 crash. Local Docker is unavailable; hosted image validation remains
  part of this release's verification.
- Validation: all 206 tests pass; TypeScript, production build and diff checks
  pass. Existing size advisory: 1,125.34 kB raw, 397.14 kB gzipped.
- Native browser: accepted the last journey, restored the roster course,
  spoke aboard, used a gate, docked at the chosen port, opened the deck
  farewell, deferred it, and paid the optional bonus. One alumnus, keepsake,
  payment and queued letter were recorded. Opened the delivered letter,
  navigated to older mail, scrolled, closed, and opened a preview by mouse.
  A later visit displayed the retired crew member's new dialogue.
- Destination approach and world state used temporary in-memory fixtures.
  The full route was not manually flown. Deck walking used held-input fixture
  state; interactions used native keys and pointer clicks. Autosaves were
  disabled and the browser unlinked. No application console errors; temporary
  tabs closed. No physical touch device or gamepad was tested.
- Released as `64bf783d3bfcb83916bdfbfbd819f84720cd1fd8`, tag `v0.237.0`.
  Remote main and tag verified. CI `34613283494` passed, including the actual
  Cloudflare deployment. Image `34613283420` passed on its first attempt; the
  build/push step took 16 seconds with the native build stage.
- Production `/assets/index-BJQrLIaC.js` matches the local build, SHA-256
  `56c430c478648976312a21dc94e23c8aa374120fdce050ab74d3f02a04ff8db0`.
  API health returned `ok: true`. The 0.237.0 catalog update is merged.

### M398 / 0.238.0: a chair for the rock

- Added a council room to civil mining/refinery promenades, with a visible
  chair, delegates, papers and kettle. Visitors can listen; a freeman with
  three belt standing can take a seat and vote.
- Water, rescue dispatch and filter certification agendas rotate each Monday
  UTC. One ballot per week survives moving seats and clock rollback. Effects
  are stated before voting; the actual resolution goes into a later letter.
  Existing faction News votes remain separate. The earlier remote tariff
  encounter is now an introduction, offered only before that vote or a seat.
- Optional representation goes to a reachable civil office of the rock's
  faction. An audience at its harbourmaster records a written response or
  an offer of a joint meeting. It does not claim a contract was signed.
- Return to the sending council to report for 300 credits and one belt
  standing. Payments and letters settle once. Older mandates persist across
  weekly agendas, save/load and detours, using no cabin or cargo space.
  Returning papers unfinished pays nothing and incurs no penalty. Standing
  loss does not block finishing an accepted journey.
- G/C and the map button restore the exact port course; C on Missions does
  the same. Flight and mission displays keep the current objective visible.
- Fixed encounter selection being overwritten by a stationary pointer and
  the promenade News terminal opening Engineer instead of News. Kiosk entry
  now updates the active scene name along with the services screen.
- Validation: 221 tests pass; TypeScript, production build and diff checks
  pass. Existing bundle advisory: 1,144.69 kB raw, 404.43 kB gzipped.
- Native browser: E into the council, mouse on the chair, keyboard agenda
  and vote, acceptance, G/C navigation, N autopilot and E docking at Meridian
  Point. P to its deck, E at the harbourmaster, mouse choice of a joint
  meeting, then a flight back to Anchor Corona with N and E. Returning to
  the council and reading the reply paid exactly 300 credits, added one belt
  standing, cleared the mandate and course, and recorded one representation.
  Checked visitor refusal after lowering standing. No application errors.
  Advanced a completed fixture to the mail delivery time and read both
  council letters through the promenade News terminal and L reader.
- World and starting positions were temporary fixtures; local deck approach
  positions were set beside the doors. The return flight between the two
  stations ran in the game. No physical touch device or gamepad was tested.
  Autosaves were disabled and no callsign/cloud save was linked. Temporary
  browser tabs were closed. In a 50 ly, 76-system fixture, council action
  generation averaged 0.82 ms over 30 calls, with a 1 ms maximum; this is
  menu generation only, not total frame time. Native News kiosk entry now
  reports scene station, tab 9, return context stationwalk.
- Released as `659b6a935e786c8e630214df1313f7ccb4fa4be6`, tag `v0.238.0`.
  Remote main and tag verified. CI/deploy `34615474417` and image
  `34615473890` passed, including the actual Cloudflare deploy step.
- Production `/assets/index-C4lBdoVP.js` matches the local build, SHA-256
  `0f6e1bd5da186aace40f4373ebcd638dea21b16c7bc9b68663aaa0b4918c6f61`.
  API health returned `ok: true`. Catalog PR #242 merged after both required
  checks passed; the guarded queue finished.

### M399 / 0.239.0: a reasonable reconstruction

- Added an original three-act sim program written by the named ship. It takes
  a stable cast from the captain, ship, crew, home port, motto and recorded
  history, with council service, farewells and rescues supplying memories.
- Three choices in each act lead to three recorded endings. Pausing preserves
  both questions and responses through later journeys and save/load. Completion
  gives current crew six morale and a small loyalty gain once, plus one
  first-run keepsake. Stale callbacks and repeated answers cannot repeat rewards.
- Added a scrollable rig menu for the new program and all seven existing
  programs. Keys, wheel and pointer reach every row. Return preserves the
  captain's position by the study; study remains available after a program.
- Added later ship and crew dialogue, handbook content, player instructions,
  release notes and roadmap entries. Existing optional save fields persist
  at save version 14.
- Validation: 240 tests pass, including 19 new sim tests. TypeScript and the
  production build pass. The bundle advisory remains, approximately 1,158 kB
  raw and 410 kB gzipped.
- Native browser: E at the rig, three acts with keyboard and mouse choices,
  pause during a response, E resume, and final completion. The program used
  Rosa Sen, The Long Way, Kettle, Mina Sol and seven rescued lives. Mina's
  morale rose from 70 to 76 once, credits stayed at 600, one ending and one
  keepsake were recorded, and E afterward trained piloting by 0.5.
- Native keyboard scrolled to the classic cat program; a pointer click ran
  it, displayed the cat result, raised morale from 40 to 47 and consumed the
  session. No application errors. Temporary worlds and rig approach positions
  were fixtures, with autosave disabled and no callsign or cloud link. All
  temporary tabs were closed. No physical touch device or gamepad was tested.

- Released as `853bd0e71673c95550ec6d32c29084358218a4c4`, tag `v0.239.0`.
  Remote main and tag verified. CI/deploy `34617521738` passed, including the
  actual Cloudflare deploy step; image `34617522402` passed.
- Production `/assets/index-fio8lJpZ.js` matches the local build, SHA-256
  `a4b5dbe31f97d9cd5a8320c90382cca58e632372a0335ab2066029e0865fa64d`. API health returned `ok: true`.
  The guarded 0.239.0 catalog queue finished; PR #243 merged at 15:45 UTC,
  commit `10757f4442df50c314022f131d66894322ecc7c7`, after required checks passed.

### M400 / 0.240.0: the duty desk

- Added a service office at the upper naval promenade. Voluntary enrolment
  requires ten deeds and ten faction standing. A service record belongs to
  one navy and remains separate from the lanes' deed rank.
- Liaison orders require a visit to a named civil harbourmaster. Patrols
  accumulate 90 seconds in the target system below speed 60, off cruise and
  clear of docking. Surveys unlock after two reports and require 60 seconds
  close to a named planet, below speed 60, off cruise and without red alert.
- Completed work is filed in person at the issuing office, for the quoted
  pay, two faction standing and one service credit. Promotions occur at two,
  five and nine reports and add 100 credits per grade to later quotes.
  Promotion letters and the last twelve actual reports persist.
- G/U and Missions U restore the current destination. Liaison and return
  courses use the exact station. Progress survives detours and save/load,
  without an expiry. Orders can be returned at any office of the same navy
  without penalty. Completed reports remain accepted below zero standing;
  new assignments wait. Posting transfers retain grade and history.
- Validation: 255 tests passed, including fifteen service tests. TypeScript,
  production build and diff checks passed. Bundle advisory: 1,177.66 kB raw,
  415.67 kB gzipped.
- Native browser: E into Fort Onyx's office, enrolment, liaison acceptance,
  E docking at Corona Hub, P to the deck, E at its harbourmaster and a
  keyboard choice to hear the dock hands. G/U plotted the exact return port.
  E docking at Fort Onyx and a pointer click filed the account: 2000 to 2550
  credits, one completed report, standing 30 to 32, and cleared course.
- A native-selected patrol ran its full 90 seconds on the game clock. Filing
  paid 700 credits and promoted the captain to watch officer, with two reports
  and standing 34. The office offered quiet surveys and queued one promotion
  letter. The lanes rank remained lieutenant. Red alert held survey progress
  at zero; native Y cleared it and the full 60-second survey completed in
  real game time. No application errors. Temporary tabs were closed.
- Test worlds, station approaches and deck positions were fixtures. Hostile
  contacts were removed around the flight-timing checks. No callsign or cloud
  link was used, and autosave was disabled. No physical touch device or
  gamepad was tested. On a 50 ly, 76-system fixture, service offer generation
  averaged 0.81 ms over 30 calls, with a 2.3 ms maximum, excluding frame work.

- Released as `9e358fd7682a808b6257ed15e0d4fb1d3b4f9685`, tag `v0.240.0`.
  Remote main and tag verified. CI/deploy `34619581748` passed, including the
  actual Cloudflare deploy step; image `34619581650` passed.
- Production `/assets/index-DGxJGoLa.js` matches the local build, SHA-256
  `92dc13a0b9b33a9fc8d6ce02e9b7595979d113189eedc5c8913a833e6a80d38a`. API health returned `ok: true`.

### M401 / 0.241.0: a cutter in your custody

- Added the playable Tern Service Cutter, issued outside the hull market at
  watch officer grade. Three crew berths, 60 cargo, 180 fuel, 160 hull and
  90 shield, with a freighter interior. One issue per filed report prevents
  repeated depot fuel claims. There is no fee or return deadline.
- Holds the original hull in a separate custody record, including name, paint,
  condition, systems, breaches, fires, fuel, ammunition, wear and history.
  The captain's crew, cargo, fittings, furnishings and ship voice move across.
- Return requires the real issuing depot and enough cargo and crew capacity
  on the original hull, including reserved leave berths. Acquired fittings
  transfer back; new ammunition adds to the original stored rounds once.
- The original ship stays outside the fleet's scrap, charter and trade lists.
  Hull purchases, swaps and liner changes are refused during the loan. Stale
  parked-hull actions are guarded. The shipyard return remains accessible if
  depot ownership changes, and falling standing does not prevent return.
- Added depot navigation, office scrolling, custody labels, handbook and player
  guide entries. Optional fields persist at save version 14.
- Validation: 269 tests passed, including fourteen custody tests; TypeScript
  and production build passed. Final copy was shortened after the browser pass
  to fit the shipyard toast and avoid repeating an article in a ship name.
- Native browser: read the issue terms, pointer selection to borrow, launch
  the cutter, and enter its freighter deck. Kettle and Mina Sol were present;
  the held hull's damaged system stayed at 63 while the cutter's was healthy.
- A fixture hold of 41 ore prevented return to the 40-space Wren, leaving
  custody and cargo intact. The native hull market refused trade-in without
  changing credits or the active hull. Purchased four torpedoes for 240 and a
  cargo rack for 900 through the yard, then returned through its first row.
  The original name and hull returned: hull 73, shield 27, fuel 44, system 63,
  cargo 41 in capacity 65, eight torpedoes, and no loan or return fee. Credits
  stayed at 3650 during return. Earlier station visits paid normal crew wages
  and a crew-request bonus; those are separate from the custody actions.
- World, qualification, original damage and cargo were temporary fixtures.
  Native keys and pointer drove the issue, yard transactions and return.
  Autosave was disabled, no callsign/cloud link was used, and the temporary
  tab was closed. No console errors, physical touch or gamepad testing.
- Catalog 0.240.0 merged as PR #244 at 16:06 UTC, commit
  `93e0f742683e5edbbc09445ebca87a7cbf95e5f1`, after required checks passed.

- Release 0.241.0: `8c1a18d838beae383f9fb4f48c40096ccebe552e` on remote
  main and tag. Hosted CI 34621245764 passed its actual Cloudflare deployment;
  container workflow 34621244347 passed. Production health was healthy.
- Production `index-BRcYSWji.js` matched the local build, SHA-256
  `95c07430742e7fc853514b7b5e00a2bf28a1f229ba6b6c2e6cbf0d2fe1b09979`.
- Catalog 0.241.0 merged as PR #245 at 16:27 UTC, commit
  `186086950e5bf0a744c310fb2974523383f1d0bb`, after required checks passed.

### M402: The Fares Already Aboard

- Added a duty-desk decision when accepted fares require another destination.
  Fares first reduces report pay by 100 without an upfront charge and holds
  service progress; orders first costs affected fares five mood once; decline
  leaves both parties uncommitted. Existing fare conditions remain.
- Snapshot the prior manifest, route its outstanding fares through G/U, and
  resume service after it closes. New fares do not extend the original promise.
- Real station hand-in and singers' arrival record delivery before missions
  are removed. Reports distinguish delivery, still aboard, and departure
  without a receipt. Saves preserve the decision and individual receipts.
- Validation: all 281 tests passed, including twelve fare-decision regressions.
  TypeScript and production build passed: 1191.63 kB raw, 420.77 kB gzip.
- Native browser: Fort Vesper offered Port Onyx liaison at 550cr while Mara
  Vale was booked to Citadel Aurora. Decline preserved credits 2000 and mood
  60. Pointer selection of fares first changed report pay to 450, held the
  order, and plotted Citadel Aurora. Native docking and Missions hand-in
  removed Mara's mission while retaining her delivery receipt. Missions U
  restored Port Onyx. Native P/E harbourmaster dialogue completed liaison;
  native office reporting paid 450 once, credits 2192 to 2642, and its visible
  last report named Mara as delivered and the 100cr amendment.
- A second temporary booking tested orders first: full 550cr quote, mood
  60 to 55, booking intact, and credits unchanged. Card text and report fit.
  An unrelated station reception occurred on return and was declined before
  visiting the duty desk. World, qualifications and approach/deck positions
  were fixtures; keyboard and pointer drove choices, docking and hand-ins.
  No autosave, callsign or cloud link; no console errors; test tab closed.
  Physical touch and gamepad behavior were not tested.

- Release 0.242.0 is on remote main/tag at
  `db9bf43cb573b4b6b2c4dbba43aa4ae801e1ecdc`. Hosted CI 34622701335 passed
  its actual Cloudflare deployment; container workflow 34622701039 passed.
  Production health was healthy, and `index-DFKTA4Y7.js` matched the local
  build, SHA-256 `b2c6e0cce3394ad271b7c35745741f88151014238ab4cb2c023445fe79a7ab8b`.
- Catalog 0.242.0 merged as PR #246 at 16:40 UTC, commit
  `b1655b1e9d4be4c73a9f66b961e02027e19d4234`, after required checks passed.

### M403: One Visit to Port

- Confirmed station menu re-entry used floor(world.time) as its settlement
  identity, repeating wages, food, passenger stops and crew dock counts after
  promenade time passed. It also reset sim, counselling and briefing state
  on every entry, including returns from an arrival encounter.
- Added explicit persisted docking visits at completed approach, recovery
  and liner arrival. Cached services keep offers and tab through re-entry;
  settlement and leg resets run once. Older docked saves adopt a settled visit.
- Kiosk Escape still returns to deck; deck Escape clears the kiosk return
  context so the services menu can launch. Arrival autosaves moved after all
  leg resets. Base/race responses check the current world and visit before
  writing scene data.
- Twelve focused tests pass: economy/experience/passenger idempotency,
  same-timestamp genuine arrivals, activity state, completed and pending saves,
  old saves, card/kiosk returns, glide/recovery/liner boundaries and stale data.
- Validation: 293 tests passed; TypeScript/build and diff checks passed.
  Bundle: 1192.71 kB raw, 421.06 kB gzip.
- Native browser: a completed E docking at Point Juniper paid one 80cr wage,
  consumed one provision, advanced Ada from four to five docks and recorded
  one passenger stop. Credits were 1920 and provisions 19. After a fixture
  time advance and activity state, native promenade and kiosk visits retained
  those values, sim use, briefing/focus and counselling. The selected shipyard
  tab remained. Kiosk Escape went to deck, deck Escape to services, and the
  next Escape launched without a charge.
- A second completed E approach at the same station settled exactly once:
  credits 1840, provisions 18, six crew docks and two passenger stops; activity
  state reset for the new visit. An in-memory save round-trip plus another
  native deck return retained this settlement and newly set activity state.
- World, starting crew/fare, approach positions, clock advance and activity
  markers were fixtures. Native controls drove docking, menu and kiosk paths.
  Autosave was disabled, no callsign/cloud link was used, no console errors,
  and the temporary tab was closed. Physical touch/gamepad untested.

- Release 0.243.0: `63fba28534fc83d09c5a98bfb051b950a13cc0d4`, remote
  main/tag. Hosted CI 34623507973 passed the actual Cloudflare deployment;
  image 34623508197 passed. Production health was healthy.
- Verified production `index-BCJgvjbl.js` against an isolated rebuild of that
  exact commit because subsequent local work had replaced dist. SHA-256
  `543c1d5d95f94e224b98204d7613be870f21729e9bc8405da86f8ef156ba8840`.
- Catalog 0.243.0 PR247 merged at 24449e7b1d156d4165a6c3d3a5a8b3769190ab66.

### M404: The Fittings Come With You

- Confirmed repeatable cargo pods and booster plates existed only as changed
  maxima and disappeared on hull changes. Shield upgrades compounded at the
  engineer but used a linear grade formula on a hull swap. Fuel was clamped
  before the fitted tank was added; purchases ignored transferred cargo racks.
- Added explicit yard capacity bonuses and a shared fitted-stat calculation.
  Old saves retain the surplus currently recorded above base fitted capacity.
  New equipment order is consistent, and fuel is clamped to the full tank.
- Hull purchase/swap/liner/cutter checks now share the fitted capacity and
  reserved shore-leave berth count. Market previews show fitted totals.
- Validation: 304 tests passed, including eleven fitting regressions. Build
  and diff checks passed. Native market preview showed the fitted Wren with
  hold 85, shield 75 and fuel 140 from a legacy-surplus freighter fixture.
  A native trade kept 80 ore, the full 130 fuel and yard bonus 20 cargo/25 shield.
- Native yard scrolling reached the pod and plate purchases. They cost 500
  and 800, producing bonuses 30 cargo/50 shield. Buying the freighter through
  the native market then preserved those bonuses: hold 195, shield 120,
  fuel 130, credits 12200. The earlier Wren had hold 95 and shield 100.
- World, equipment and old capacity surplus were temporary fixtures; native
  controls drove yard and hull purchases. No autosave/callsign/cloud link,
  no console errors, and the temporary tab was closed. No physical touch or
  gamepad check. Shield-grade and cutter-custody combinations passed tests.

- Release 0.244.0: `6bf0bf15b4d77f25e2171ed2d7086c403748dd25`, remote
  main/tag. Hosted CI 34624392695 passed actual Cloudflare deployment; image
  34624393379 passed. Production health healthy; `index-Bb1lvgnk.js` matches
  the preserved release bundle, SHA-256
  `ee5ee3ce6ae2761ff113e6c961dcf20ce10a995c89fd2c0134ff6a7eb62b16ed`.
- Catalog 0.244.0 PR248 merged at 15bb5c0435cc25f2ac1acd8a30d20e7181161360.

### User direction: end attack-on-sight loops

The user requested a way to stop being attacked on sight, or a cooldown.
Prioritize this after the current fitting checkpoint. Confirmed updateLaw
only reduces wanted heat below the pursuit threshold; above it the timer
reduces reputation every 60 seconds even without a nearby pursuer. Patrols,
fighters and platforms also use inconsistent hostility predicates. Add an
observable break-contact cooldown and accessible warrant settlement, with
regression and native pursuit checks. An optional question about attacker
type is pending, but the law loop can be fixed independently.

### M405: The Pursuit Ends

- Replaced the pursuit reputation-loss timer with a 60-second out-of-contact
  cooldown. Severe wanted heat and old outlaw standing can both clear. Closed
  cases, active cases and elapsed quiet time persist without a schema bump.
- U in flight and the second pause action open traffic control. Exact quoted
  restitution closes open cases and restores their standing to neutral or
  better. Insufficient funds, stale world/system/record and duplicate payment
  cannot consume credits. A free escape option is always available.
- Free cooling keeps reputation consequences but restores civilian docking.
  Military docking still uses standing. A new recorded offence reopens the
  faction case; returning to an old closed case does not.
- Patrols, fighters and law platforms use the same law predicate. Settlement
  withdraws law projectiles while preserving unrelated pirate shots. Existing
  amnesty and records-clerk actions also close saved cases. Guarded jumps
  preserve the departing jurisdiction before replacing the flight population.
- Pause actions scroll within nine visible rows; keyboard selection survives
  a stationary pointer, with a separate footer.
- Validation: 321 tests passed, including 17 new law and menu regressions.
  Build passed: index-Cmlj-PSF.js, 1,199.10 kB raw / 423.69 kB gzip.
- Native U opened a severe warrant beside a patrol; the 3550cr quote changed
  10000cr to 6450cr, heat 1 to 0 and faction standing -85 to 0. Same contact
  remained. Pause -> Down -> Enter reached the same channel; a zero-credit
  payment attempt left the warrant open and offered the free route.
- Native free choice followed by an out-of-range position fixture ran without
  clock acceleration. At 38.9922 flight seconds the HUD showed 22 seconds left;
  the log closed pursuit at 60.0092 seconds. At 65.7755 seconds: heat 0, law 0,
  credits 0, standing -85, closed case fdm. No timer reputation loss.
- An in-memory migrateSave round-trip preserved that closed case. At Fort
  Bastion a nearby patrol hailed the ship as clean and did not fire, while the
  military station refused the poor standing. A 2750cr fixture funded actual
  U/Enter restitution; native E completed docking at sys0-st0 (scene station).
  No console errors; the temporary tab was closed. Physical touch/gamepad untested.
- Temporary world, starting record, credits, contact positions and save copy
  were fixtures; native keys drove the menus and docking requests. No autosave,
  callsign or cloud connection. Civilian reopening, all three weapon types,
  new-offence reset and saved cross-system cooling are covered by tests.
- The first optional native civilian-port lookup found none for fdm in this
  seed and threw in the fixture setup. Corrected by using the existing military
  port; no product exception or source change resulted.

- Release 0.245.0: `02ca9484696322b59f28c76a36a202c7224824e4`, remote
  main/tag. Hosted CI 34626226755 passed actual Cloudflare deployment; image
  34626229580 passed. Healthy production serves index-Cmlj-PSF.js, matching
  SHA-256 `2164e81b724a839a9a25dd432c0ed10c22504424e0993fb3c372c84407e5b480`.
- Catalog 0.245.0 PR249 merged at c5e2c29cf3c81e9f1979b391009af82dae1bc4b2.

### M406: They Keep Their Word

- Confirmed parley only scattered the current nearby ships. New corsairs could
  immediately intercept, while automatic gunners/drones ignored friendly Veil
  standing. Pirate platforms also ignored friendly status, and E could force
  another parley over a nearby station docking action.
- Added 180 flight seconds of saved, system-specific safe passage after any
  successful parley. Replacement corsairs honour it. Flight anywhere consumes
  time; menus and docking do not. Gun or torpedo hits end temporary passage.
- Pirate platforms, escorts, auto-gunners, warning chatter, threat markers and
  automatic yellow alert now agree with the same peaceful predicate. Existing
  trusted Veil standing uses this path. Passage withdraws hostile corsair and
  player/escort projectiles and torpedoes, preserving law and bystander fire.
- Parley callbacks guard world, system, original participants, eligibility and
  repeated resolution. Peaceful contacts do not demand another toll or block E
  docking. Fifteen focused tests passed; 336 tests passed overall. Build passed with
  index-Du1zG5ZM.js, 1,202.84 kB raw / 425.06 kB gzip.

- Native E parley with two corsairs quoted 240cr. Enter changed 1000cr to
  760cr, put both hulls into retreat, cleared outstanding projectiles and
  granted 180 seconds. A replacement corsair plus an escort were introduced.
  At 60.6748 flight seconds: 119.1835 seconds left, no shots, alert green,
  hull 100/shield 50, replacement hull 100, escort hull 90.
- Native Escape paused at 90.9086 seconds remaining. It stayed exactly there
  during the pause and after an in-memory migrateSave reload. Resumed normal
  flight consumed the rest. At world time 183.0581 passage was gone and the
  channel announced expiry; hull/shield and 760cr stayed unchanged. No clock
  acceleration was used for the three-minute test.
- A fresh single-corsair native E/Enter agreement cost 120cr (760 to 640).
  Native R, with the target position aligned to the gun aim as a fixture,
  fired one manual torpedo and hit. Passage cleared, ammunition 2 to 1, and
  the toast explicitly said our torpedo ended the agreement.
- World, crew, contacts/positions, initial credits and save copy were fixtures.
  Native keys drove parley, pause, payment, return and torpedo launch. No
  autosave/callsign/cloud connection, no console errors, temporary tab closed.
  Physical touch/gamepad untested. Friendly Veil platforms, all success paths,
  failed/replayed/stale calls and E port access are covered by tests.

- Release 0.246.0: `0565b3254890559f3489ae0fea7451f2a81dc592`, remote
  main/tag. Hosted CI 34627416307 passed actual Cloudflare deployment; image
  34627416469 passed. Production index-Du1zG5ZM.js matched the preserved
  build, SHA-256 `821220f43ce0a6351161065381201b6aaa1d8378999a6cc4e89015fbb8ab082f`.
  Production health healthy. Catalog 0.246.0 PR250 merged at
  ec49a83dbcc2b56cb0d0d1114ab28d7c7beb6324.

### M407: Every Hull on the Register

- Confirmed the seven market entries put parked hulls below the 270px canvas.
  Remote and working ships were non-selectable text. R released the last
  charter and L took the nearest remote hull regardless of intended ship.
- Added a window of seven rows for market/local/remote/working entries and a
  separate details column with fitted capacities, costs, locations and controls.
  L/Enter target the selected remote hull; R targets the selected charter.
- Confirmed shipyard rowBoxes used visible indices after scrolling, so a pointer
  purchase could act on a different item. Explicit indices now preserve the
  mapping. Details-column clicks no longer activate list actions.
- Ten focused tests passed: long-fleet reachability, actual pointer dispatch,
  specific liner destination, charter settlement/return once, refusal paths,
  and the scrolled shipyard purchase. Full suite: 346 tests passed. Native
  register displayed 28 entries with a separate, legible details column.

- Native Down reached parked OWNED 10 at row 18/28, then a pointer click on
  that visible row boarded it: hull 80, torpedoes 10, credits 20000 unchanged.
- Native R on FIRST WORKER showed its 51cr settlement and Point Ballad return
  depot. Confirming raised credits to 20051, returned that hull at 80 health,
  and preserved SECOND WORKER and LAST WORKER.
- Native L on remote OWNED 16 quoted Vesper Rest, Tauith, four jumps, 680cr.
  Confirming boarded OWNED 16 at sys2-st1 with 16 torpedoes. OWNED 17 stayed
  remote. Credits 19473 include the fare and both remaining charters' 51cr
  docking receipts.
- Native shipyard Down reached cargo-pod option 35 after a 17-row offset.
  Pointer purchase charged exactly 500cr, raised cargo 40 to 50 and added
  explicit yard cargo bonus 10; modules stayed empty. No unintended purchase.
- World, owned/working hulls, credits and settled arrival were fixtures.
  Native keys and pointer drove scrolling, transfer, confirmation, liner and
  equipment purchase. No autosave/callsign/cloud link, no console errors,
  temporary tab closed. Physical touch/gamepad untested.

- Final build passed: index-DnQQo-Nw.js. Current-hull details say it is already
  aboard; fresh native rendering verified that wording and the tab was closed.

- Published v0.247.0 / e1e183c579b613fb5a6f4e18ac3e336445a8c33d.
  CI 34628375634 including actual Cloudflare deploy and image 34628375739
  passed. Production index-DnQQo-Nw.js matches preserved local SHA-256
  8a06da4433bf84483ff24fbadf0ea3a7252ae952b7ab2e4aa3b1d1e75963b3c6;
  /api/health returned ok. Catalog 0.247.0 PR251 merged as
  ca3883a0226585b0be8996d5b1bb1bc34eb47cad at 17:44 UTC.

### M408 / 0.248.0: Find the Page

- Confirmed native Controls clipped ON FOOT and hid EVERYWHERE. Shared reader
  now scrolls to the end, jumps headings, pages, searches titles/body text and
  keeps complete sections. Explicit pointer buttons replace click-anywhere exit.
- Both references open from flight pause and return to its current population.
- 356 tests passed, including ten new reader and flight-continuity checks.
  One initial assertion assumed more than 50 handbook sections; corrected it
  to compare the complete source list. Build passed: index-CChYORcP.js,
  1210.00 kB raw / 427.94 kB gzip.
- Native Controls End reached scroll 207 and rendered ON FOOT and EVERYWHERE
  fully. Text clicks kept the page open; Escape preserved all original contacts.
- Native window.prompt did not appear for key or pointer search in this host.
  Replaced that dependency for the new search with an in-page HTML dialog,
  labelled search input and Find/Cancel buttons.
- Native S, text entry and Find returned both CORSAIR SAFE PASSAGE and ENDING
  A PURSUIT for warrant, with complete text. Clear restored 34 sections.
  Right and PageDown reached scroll 231; Cancel preserved that position and
  query. Enter submitted an unmatched query and displayed clear recovery text.
- Native End reached scroll 1862 and the final THE SERVICE AND THE BELT lines.
  World time stayed exactly zero during the handbook test; pointer Back
  returned to flight with all contact identities preserved and no residual
  dialog. No console errors, temporary tab closed, no save/callsign/cloud write.
  Physical touch/gamepad input untested.
- Published v0.248.0 / 031b769e5223f830d213700b32730507141b78d4.
  CI 34629790664, actual Cloudflare deploy and image 34629791400 passed.
  Production index-CChYORcP.js matches preserved local SHA-256
  55f58ea8f23be6e17e3a4454767eb9601e3cfa0cf9363e41dc48c2402feef065;
  /api/health returned ok. Catalog PR252 merged as
  0037096ceeef8e7e4fc41abb2b9fa37c1d18e17e.

### M409 / 0.249.0: One Conversation at a Time

- Reproduced a crew arrival card immediately overwritten by a due hearing.
  Port audiences now wait until the open crew/envoy card returns. Optional
  portAudiencePending lives on the saved visit; stale world/visit checks keep
  it attached to that arrival. Existing settled visits gain no new audience.
- Four new regressions, 360 tests passed. Updated an older autosave-count
  assertion to allow saving the deferred audience while preserving the stronger
  credit/food/crew/leg invariants and checking further menu returns do not save.
- Build passed: index-t3w4Zoe6.js, 1210.98 kB raw / 428.24 kB gzip.
- Native seed 409 fixture, time 1, engineer Ada Vale with low morale and a
  due hearing: real crew-leave card opened first. Arrival charged 80 wages,
  used one food, raised crew docks 4 to 5. Hearing count stayed zero.
- Native Enter paid the chosen 150cr bonus: credits 1920 to 1770. Continue
  opened hearing exactly once. Food stayed 19, docks 5 and credits 1770.
  Answering and continuing returned to station with the same totals.
- In-memory migration of the pending pre-answer snapshot resumed the port
  hearing, kept already-settled credits 1920/food 19/docks 5, and cleared the
  pending audience. This verifies the port queue, not resuming an unanswered
  crew card across reload. No console errors, tab closed, no persistent saves.
- Published v0.249.0 / cf497ad814f5b465c152a7eef13f41bc431f466e.
  CI 34630215886 including actual Cloudflare deploy and image 34630215732
  passed. Production index-t3w4Zoe6.js matches preserved SHA-256
  81fa016cc99cc8b1c3f9bae8e1e79702d18d5380a079cd4a6ad19419b2e61baa;
  /api/health returned ok. Catalog PR253 merged as
  e7543b7e2711164d82312482c0e402267bf3b17f.

### M410 / 0.250.0: The Whole Service File

- Confirmed native Service Office showed only six of twelve lines in a long
  report and offered no way to open eleven older retained reports.
- Added service file using ReaderScene. Current orders, complete fare receipts,
  cutter custody and every retained report are readable/searchable. Reports
  are newest first, with actual filed stardate and payment.
- Five new tests, 365 total passed. Tests cover data preservation, all reports,
  full fare receipts, custody, world replacement, empty file and office return.
  Corrected the initial stardate call to use its World parameter.
- Build passed: index-WnVxJUlB.js, 1213.37 kB raw / 429.06 kB gzip.
- Native archive opened through the actual office action. Twelve reports with
  75 receipt sentences each: End reached scroll 4186 and END OF REPORT 1.
  Search for end of report 7 returned only report 7. Escape returned to the
  duty desk with the complete player snapshot unchanged.
- Native current orders displayed 700cr pay, all 30 delivered fare receipts
  and WATCH 1 cutter custody. Credits stayed 600, completed work 12, cutter
  and orders stayed in place. A later whole-snapshot comparison differed;
  fresh per-field inspection isolated achievements added by the global loop,
  with no other player field changed. No console errors, tabs closed, no
  persistent save/callsign/cloud writes. Physical touch/gamepad untested.
- Published v0.250.0 / 401d976d4401d3e67d00bcd5c32dd8ffe7847026.
  CI 34630844558 including actual Cloudflare deploy and image 34630840796
  passed. Production index-WnVxJUlB.js matches preserved SHA-256
  844acd6b3bef5fb5b547489efb01723a1efa3038a72313310fde5e357b5b09a3;
  /api/health returned ok. Catalog PR254 merged as
  bf543ed34b0092debd42763405182e9a7a5ca199.

### M411 / 0.251.0: The Back of the Warehouse

- Native old warehouse with 26 goods held and 26 stored placed the selected
  final row at y653..663 in a 270px canvas. Added a twelve-row scroll window
  with actual row indices, capacity and transfer direction.
- Loading preserves the selected warehouse commodity if a new hold row is
  inserted. Empty quantities do not add selectable rows. One-unit transfers
  preserve other goods and station stores; full hold does not consume stock.
- Six new regressions; 371 tests and build passed. Bundle index-CMuE-ZZ4.js,
  1214.96 kB raw / 429.66 kB gzip.
- Native Down 51 times reached Thal Root row 52/52 at y203..213. Clicking
  loaded that item: hold 2 to 3, warehouse 3 to 2, provisions unchanged.
- Native empty-hold fixture loaded med into a new row and kept med selected
  in the warehouse. Full hold rejected another load without changing either
  quantity. Up/Up/Enter stored the final held unit; med total stayed two.
- No console errors, tab closed, no persistent save/callsign/cloud write.
  Physical touch/gamepad untested.
- Published v0.251.0 / 613793c63f61f651c731eaf92fe22069f9798884.
  CI 34631404869 including actual Cloudflare deploy and image 34631403401
  passed. Production index-CMuE-ZZ4.js matches preserved SHA-256
  00635324c0fdf30f8410e5ece7cb7d537e6371dccf7455a333edf40f86ed8f98;
  /api/health returned ok. Catalog PR255 merged as
  71c0054b333d2ecc12484a264c4a17cf301658c5.

### M412 / 0.252.0: Every Posting on the Board

- Confirmed native selection 11/12 while only six posting rows were drawn.
  Added a seven-row board window with actual indices for pointer actions.
- J or Full log opens full objectives, community goal, all active missions,
  all postings and progress/terms. Reading returns to the same board selection.
  Unavailable live goal progress is stated directly rather than shown as zero.
- Seven new regressions; 378 tests and build passed. Final bundle
  index-9nDpjZkt.js, 1217.24 kB raw / 430.50 kB gzip.
- Native Down 11 reached POSTING 11 at row 12/12. J displayed every long
  instruction and the final sentence, destination, base pay and posted state.
  No acceptance while reading. Escape returned to tab 3 and its selection.
- Native pointer accepted only board11, adding one provisions. Native Up 11
  reached its ready hand-in; Enter paid 111cr (600 to 711), removed the job
  and its one cargo. Other postings stayed unaccepted.
- Native eight-active-mission fixture included all eight in the full log.
  Pointer Full log and native search found ACTIVE JOB 7, including singer
  light reward. Escape preserved the complete player snapshot.
- The singer fixture retained an old human station target and exposed the
  deliverability helper disagreeing with the already-protected hand-in path.
  missionDeliverable now rejects singers at human stations. Native recheck
  showed zero ready rows and an accepted fare with 30 light at home.
- No console errors, tabs closed, no persistent save/callsign/cloud writes.
  Physical touch/gamepad untested.
- Published v0.252.0 / aca6ee4be84e2c77f0485fefd86c350d32154d14.
  CI 34632161029 including actual Cloudflare deploy and image 34632161663
  passed. Production index-9nDpjZkt.js matches preserved SHA-256
  404b42a9dee0b536c265f57d535692c070560726ab0f0bc4c4d26044caa7ea55;
  /api/health returned ok. Catalog PR256 merged as
  4fe9fcd4cbc0c6913a88208ee3000553a39407e1. Required validate/tags-resolve
  checks passed and remote main's manifest references 0.252.0.

### Final bundle integration check

- Served the exact v0.252.0 dist on fresh localhost:5198, with no existing
  save. Disabled autosave before native New game (Sol neighbourhood).
  Native K skipped flight school. The generated world ran ordinary AI.
- Temporary wanted/standing/credit fixture: U opened Traffic control, native
  Enter settled the 3550cr quote (5000 to 1450), cleared wanted, restored
  tsc standing -85 to 0 and preserved existing contact identities.
- Native pause navigation opened the Handbook. S and the in-page field found
  pursuit instructions; Escape returned to the same flight.
- Free-cooling fixture restored wanted 1, standing -85 and placed the ship
  clear of law contacts. Native U chose free cooling. The ordinary singer
  encounter opened at 49.415 seconds and held the timer while on screen.
  Native Enter answered and continued the conversation.
- A temporary observation wrapper paused after the real cooldown completed,
  without advancing time: 60.0181 flight seconds, 155.096 wall seconds with
  the story pause. Credits 1450 and standing -85 stayed; wanted cleared,
  tsc stand-down true, hull 100 and shield 50. Restored the original update.
- The generated Sol fixture had no civilian port, so a further native civilian
  docking check was not performed here. Earlier law tests cover eligibility.
- No console errors, zero autosave attempts, no local save, tab closed.
  Stopped the preview and restarted the dev server so its version is current.






### Final checkpoint

M393-M412 are released with hosted CI, actual Cloudflare deployment,
container-image and exact production-bundle verification. BoxPilot catalog
is merged through 0.252.0. The initial successful catalog backlog was handled
sequentially; cancelled image 0.222.0 was superseded. No Bigbox installation
or service restart was performed.

The current source overview, release procedure, state boundaries and user's
pursuit request are in the rewritten docs/HANDOFF.md. Local dev remains at
http://127.0.0.1:5199. The temporary release preview was stopped. Browser
fixtures did not write saves or create a callsign/cloud link, and all test tabs
are closed. Physical touch/gamepad and VoiceOver remain untested.

The authorized window ends at 18:34:39 UTC. Pause the continuation at that
point, and do not start another implementation slice without a new request.

### M413 / 0.253.0: Keep Your Place (follow-up after the six-hour window)

- User reported shipyard and market scrolling returning to the top. Native
  wheel events with one-pixel pointer movement reproduced cursor 2, 1, 1, 1.
  The market also rendered its last of 26 goods at y291-301, outside the canvas.
- Station lists now change pointer selection only on a click. Wheel and
  keyboard selection survive pointer motion, including after scrolling stops.
- Market rendering uses twelve visible rows with actual item indices. The
  range count and information stay below the list; information cannot trade.
- Seven regressions added. Six failed before the fix; all seven pass after it.
  Full suite: 385 passing. TypeScript and production build passed. Bundle
  index-Bk73yNWT.js, 1218.20 kB raw / 430.84 kB gzip, preserved SHA-256
  6185b087c51aa88d62d89549a085958d7919dc1e7700a581c9afde057b399c9e.
- Native production-preview wheel testing with pointer movement reached
  shipyard cursors 10, 20, 30, then final row 36/37. Clicking its shield
  booster charged 800cr and raised shields 50 to 75, leaving cargo capacity.
- Native market wheel testing reached cursors 10, 20, then final row 25/26.
  Thal Root stayed visible at y165-175; subsequent pointer movement preserved
  selection. Clicking sold exactly one root for 98cr, leaving other goods.
  Clicking information below made no trade. Twenty-five native Up presses
  reached the first row; pointer motion preserved that keyboard selection.
- No browser console errors. Temporary fixtures disabled autosave, used no
  callsign/cloud link, and are closed after verification. Physical
  touch and gamepad were not exercised. The earlier heartbeat stays paused.
- Published v0.253.0 / 8f809536f9d4ab24253cee9eb4594169a845cb2d.
  Hosted CI 34634759782 including actual Cloudflare deploy, and image
  34634761091 passed. Production index-Bk73yNWT.js matches the preserved
  SHA-256 above; /api/health returned ok. Catalog PR257 merged as
  dc6dbf4382d33e660413c7b6bb0cd6792eb04673 after validate and tags-resolve
  passed. Remote main references the exact 0.253.0 image.
- The exact native-tested preview used index-Bk73yNWT.js and had no save.
  Closed the temporary tabs and stopped preview5198. Restarted dev5199 at
  0.253.0. The six-hour continuation remains paused.

### M414 / 0.254.0: Know Your Heading

- User asked for clearer-looking ships and a visible facing direction. The
  old procedural sprite had similar taper at both ends and no persistent
  distinction between facing and actual motion.
- Eight class profiles now define the hull outline. Bright forward glass and
  a white nose, plated wings/cargo sections, paint accents and twin engine
  banks give consistent orientation. Player, ghosts, NPC classes and yard
  previews use the same design generator. No hull statistics or saves changed.
- White forward chevron follows the hull angle independently of turret aim.
  A hollow amber diamond follows velocity, with space between aligned marks.
  The HUD labels nose/drift bearings with screen-up as north. Below 2 m/s,
  unstable drift direction is suppressed. The system-map arrow follows heading.
- Flight records actual forward and retro burns for drawing. Exhaust uses
  the engine-bank position for the current hull, including a braking burn;
  coasting has no drive flame. Existing acceleration and steering are unchanged.
- Nine direction regressions cover compass conversion, wrapping, sideways
  and reverse drift, zero/low speed, every hull and zoom range. Full suite:
  394 passing. TypeScript and production build passed. Bundle
  index-COueUCNG.js, 1223.13 kB raw / 432.89 kB gzip; preserved SHA-256
  8eb547a9a3eb37373f69606cb224953a01fa0c7d78be715cb2bc80f1d77b6bdf.
- Browser review inspected all eight hulls at detail and flight sizes. Exact
  production preview verified a north-facing scout drifting east, with
  separate matching nose/drift cues. Carrier at zoom 2 showed northeast nose
  and southwest drift; native wheel reached minimum zoom 0.25 with a readable
  white chevron. Native Tab showed the oriented map arrow. Native pointer
  movement set turret aim north while hull heading stayed west.
- Held controls were exercised through temporary Input.down state against
  ordinary animation frames. Native pressKey releases too quickly to span
  an isDown frame in this host, so this is not a physical held-key playtest.
  A one-frame observation pause captured actual forward and retro burn
  rendering, then the original update was restored. Coasting extinguished
  both drive states. Turning changed angle from -1.5708 to 0.4128 while
  velocity remained (80, -0.2385). Braking turned against that motion and
  reached speed zero, with 102 observed burn frames and no flame after stop.
- No browser console errors or local save. Temporary fixtures disabled
  autosave and created no callsign/cloud link. Physical touch/gamepad and
  subjective held-key handling remain untested. Earlier continuation stays
  paused; this feature is the separately authorized follow-up.
- Published v0.254.0 / c2cf48be65bc2206e93bca783cdff44d8400cb08.
  Hosted CI 34636708007 including the actual Cloudflare deploy and image
  34636707994 passed. Production index-COueUCNG.js matches the preserved
  SHA-256 above; /api/health returned ok. Catalog PR258 merged as
  fb09e210e33d1b613586477772635995fc9e9de1 after validate and tags-resolve
  passed. Remote main references the exact 0.254.0 image.
- All temporary browser tabs are closed and the preview on 5198 is stopped.
  Dev5199 was restarted at 0.254.0. No persistent fixture or save remains.

### M415 / 0.255.0: All the Settings

- User reported that Settings could not scroll down. Native reproduction
  showed 34 rows drawn down to y349 on a 270px canvas, no wheel handling,
  and pointer hover overriding keyboard selection. A stale six-row offset
  also put the bindings heading above ordinary options.
- Settings now shows fourteen entries at a time, retaining actual row indices
  for pointer actions. Wheel and arrows keep the selected row visible;
  Page Up/Down, Home/End and pointer page buttons reach the whole list.
  Pointer movement cannot overwrite selection. Section headings follow the
  actual row type. A range count and scroll indicator show position.
- Clicks use the same list bounds for selection and activation. Margins,
  section headings and footer information are inert. The Back button returns
  to the caller; while rebinding it becomes Cancel and preserves the old key.
  Left/Right volume adjustment and automatic settings persistence remain.
- Ten new regressions cover all rows, wheel/pointer drift, arrow wrap,
  paging, section placement, actual scrolled option and binding actions,
  reset, inert clicks, both cancellation paths, volume and return routing.
  Full suite: 404 passing. TypeScript and production build passed.
- Exact production-preview asset: index-DXes6cu-.js, 1225.68 kB raw /
  433.79 kB gzip. SHA-256:
  6c8e6a97ed9ef923c5132e8d9cac1a94d2a229057c3828a9b43d87fc4eff4cb7.
  The existing Vite bundle-size notice remains.
- Native wheel input with pointer drift traversed indices 1 through 33,
  then stayed at the bottom. Every selected hit area remained inside the
  canvas. Native End, Home, Page Down, pointer Page Up and arrows worked.
  A margin click made no change. Clicking the scrolled fullscreen binding
  and pressing B produced B; Reset restored F. Escape and pointer Cancel
  both left the binding unchanged. Effects adjusted to 90% and back to 80%.
- Native Escape returned to the title. A temporary world then verified
  opening Settings from flight pause, reaching the final row and returning
  to flight with Back. No browser console errors. Test writes were captured
  only in memory, autosave was disabled, and stored settings were unchanged.
  No save or cloud link was created. All temporary tabs are closed.
  Physical touch and gamepad were not exercised. The heartbeat stays paused.
- Published v0.255.0 / 9bef5b4540c4393de3654a710053bf6d0b66fb06.
  Hosted CI 34639654631 including the actual Cloudflare deploy, and image
  34639654701 passed. Production index-DXes6cu-.js matches the preserved
  SHA-256 above; /api/health returned ok. The release tag and remote main
  resolved to the tested release commit before the documentation checkpoint.
- Preview5198 is stopped. Dev5199 was restarted at 0.255.0.
- Catalog PR259 is open at f432d8abb2b19efa53a2109cc354503308b4b64a.
  The validate check passed, including npm run check. Catalog image run
  34639887745 failed on its first attempt and retry because the unchanged
  minio/minio:RELEASE.2025-09-07T16-13-09Z returned HTTP 401. FarSpace
  0.255.0 resolved with HTTP 200 on both runs. A separate authenticated
  anonymous-pull token request also left MinIO manifest resolution at 401.
  The PR remains open; remote BoxPilot main still references 0.254.0.
  Resume after the registry check passes. Bigbox was not restarted or deployed.

## M416: The Opening View, implementation checkpoint

- Implemented three title compositions using the existing hull and sprite
  generators. A separate presentation deck shows each once per cycle and
  prevents consecutive repeats across session reloads.
- Replaced the flat canvas list with grouped HTML controls, native focus,
  narrow-screen scrolling and scene-owned cleanup. Child menus restore the
  view, page, focus and scroll. Title navigation pauses voyage story updates.
- Returning-player previews read the selected save; malformed and future
  saves do not expose Continue. Cloud checks time out after ten seconds and
  late responses cannot replace a cancelled action or different slot.
- Full suite: 432 passing. TypeScript and production build passed. Preserved
  build: /private/tmp/farspace-title-20260911/m416. Assets:
  index-CItg0JfA.js, 1236.57 kB raw / 437.30 kB gzip, SHA-256
  b13728f4df8e85cdeee6ad74f0a1e6f04d894cea816138cff88ed845a6202167;
  index-DLwGHPCy.css, 3.68 kB raw / 1.29 kB gzip, SHA-256
  24cedb831f94187ee7d3f4b1266b1d7e5d61324f770628c19e261646023bb73f.
- Native checks covered all three views, Settings return focus, 320x568
  scrolling, doubled menu text, long ship/callsign text and form key ownership.
  Synthetic save/identity writes were intercepted in memory. User storage
  was not changed. An unaccelerated title idle check started at 21:05:53 UTC;
  its twenty-minute acceptance check and release publication remain pending.

## M417: A Clear Save Library, implementation checkpoint

- Replaced the old slot scene with title subpages for read-only previews,
  explicit slot choice, copy destinations, clear, recovery and export.
  Slot browsing no longer saves a generated placeholder or publishes scores.
- Added one playable-save validation path shared by local loading, preview,
  cloud and file import. Version-zero migration remains covered; future
  versions and unusable required fields are rejected before adoption.
- Replacement writes a per-slot recovery copy first. A failed recovery or
  replacement write keeps the previous save. Restore swaps the stored bytes;
  exports can retain damaged originals. Each slot keeps its cloud code.
- Cloud choices show local and remote times, expose local continuation after
  failure, and reject changes to the slot or its bytes while a choice is open.
  Imported persistence completes before the active world changes. Save and
  quit stays paused if the local write fails.
- Full suite: 471 passing, including 32 save-library tests and seven further
  native-menu regressions. TypeScript and production build passed.
- Final assets: index-C43i7EnP.js, 1242.31 kB raw / 438.79 kB gzip,
  SHA-256 204f2017fc59aec17c1a91478a970299c1cad3aadc05b352b82781fd38d3d2ba.
  CSS remains index-DLwGHPCy.css with the M416 hash above. The final build is
  preserved in /private/tmp/farspace-title-20260911/m417.
- Native checks verified preview without writes, selected-slot independence,
  copying and restoring exact synthetic voyages, and per-slot cloud ownership.
  The production build continued locally after a simulated cloud failure,
  removed the title UI on entering flight and selected a new view on return.
  A simulated quota failure kept Save and quit paused with the same in-memory
  ship and previous stored bytes. There were no browser console errors.
  All save and identity changes stayed in temporary in-memory fixtures.
- M416 and M417 will publish together as v0.257.0 after the title idle gate.
  M418-M427 remain planned. Physical touch, physical gamepad and screen-reader
  operation have not been exercised in this slice.

- Final compatibility review added the service cutter to playable-save hull
  validation. Its active save now exposes Continue in the final production
  build. All 471 tests pass, including that regression.
- The M416 frozen title build completed 1203.133 real elapsed seconds, with
  1202.998 seconds of title animation. World JSON and saved bytes were
  unchanged; no local writes, traffic entities, sprite-cache growth or scene
  rotation occurred. No browser console errors. The title idle gate passed.

## v0.257.1: Save-code compatibility

- v0.257.0 published at 4271ecc3b139f2c8adad7acfac8cf590347d2c4c.
  CI 34649382110 passed, including the actual Cloudflare deployment;
  container workflow 34649382055 passed.
- Final cloud review found that the existing generator included digits 8/9,
  while the save API and link form accept 2-7. Generation now uses only
  accepted characters. Every possible random-byte value is covered, along
  with storage failure during code creation. Existing valid codes are unchanged.
- Full suite: 473 passing. TypeScript and build passed. Native creation in
  the final production build produced an API-valid code, captured in memory;
  the browser's real cloud-code key remained empty. No cloud save was sent.
- Final JavaScript: index-CnOESIfc.js, 1242.31 kB raw / 438.78 kB gzip.
  SHA-256: 1aab1fd10f95a2813d711dd1f7bc9a2fe210ca87644dc8b23a8c8c7f0d4a7704.
  CSS: index-DLwGHPCy.css, SHA-256
  24cedb831f94187ee7d3f4b1266b1d7e5d61324f770628c19e261646023bb73f.
  Preserved preview: /private/tmp/farspace-title-20260911/m417.

- Published v0.257.1 / 28bb0ef4f40a9d5d369fa014ac28f97cb1f376bf. Hosted
  CI 34649585007 passed, including the actual Cloudflare deployment;
  container workflow 34649584851 passed. Production HTML references the
  final JS/CSS assets above, both hashes match, and /api/health returned ok.
  Native production verification showed v0.257.1, changed scenes and no
  console errors.
- Catalog PR259 now targets 0.257.1 at
  8879c0ab68aca73ccd31a49b7614ca26b8a85375. Local npm run check passed with
  1,632 tests. Registry run 34649742916 resolved FarSpace with HTTP 200 and
  failed on unchanged minio/minio:RELEASE.2025-09-07T16-13-09Z with HTTP 401.
  The PR remains open. No Bigbox install or restart was performed.
- Temporary browser fixtures are closed and preview servers 5197/5198 are
  stopped. Dev5199 is running at 0.257.1. The continuation heartbeat remains
  paused. M418-M427 remain in the approved plan.

## Website preview description

Replaced the page description with "Explore star systems, trade cargo, and
build your fleet." Added matching Open Graph and Twitter title and description
metadata for shared links. The description contains no hyphens or dash
punctuation and does not describe the visual style. TypeScript and the production
build passed. Parsed the built HTML to verify that each description appears
once and has identical wording. This is a website metadata change; the game
version remains 0.257.1.


## v0.258.0: Maps and derelicts

- Galaxy and system maps now separate chart space from the details panel.
  Zoom is anchored at the pointer; right drag or arrow keys pan. Home fits the
  chart. Labels avoid other labels and markers. Galaxy F searches names;
  inspecting a star does not change the plotted route. Route previews show
  jumps and fuel. Details scroll without changing the selection or zoom.
- The system map has contact filters and a complete scrolling list. Local
  destinations track moving objects and use their actual interaction ranges.
  Unavailable targets stop autopilot. Map input owns its keys and wheel, and
  the map pauses world and encounter clocks.
- Added seeded boarding decks with six connected compartments. Layouts vary
  in width, depth, room assignments, door positions and airlock side. Panels
  and cargo racks stay clear of doorways. Room names stay outside cargo rows.
- Repair panels retain progress on the live NPC or station tender. Completed
  NPC repairs clear the work state but retain the seed for a subsequent fault.
- Wrecks support power restoration, sealing breaches with parts, fuel recovery,
  recorder recovery and survivor evacuation alongside existing salvage. Power
  is required to release a survivor. Cargo, hazards and completed actions stay
  on the wreck across visits and saves. Completion materials are granted once.
  Returning from a boarding encounter keeps the chosen companion and deck.
- Derelict anomaly interactions create one persistent boardable wreck. The
  chart retains it while recoverable contents remain. Old cleared wrecks do
  not gain new resources when read by this version.
- Checks: 494 tests, TypeScript and build passed. Reachability tested across
  300 repair seeds and 150 wreck seeds with unopened cargo. Additional cases
  cover capacity failure, oxygen work, persistence, route isolation, map
  scrolling, selected targets, vanished destinations and derelict conversion.
- Browser: found Ross 248 by typing its name, selected and zoomed around it,
  plotted a route, and inspected the system contact list. A repair walk through
  the real frame loop took 19.657 seconds, restored all three systems, and
  returned to the original freighter with its disabled state cleared. The
  production build's wreck walk restored power, rescued a survivor, recovered
  15 fuel, a recorder and both crates, and sealed both breaches. After leaving,
  the wreck was cleared and flight contacts were unchanged. A JSON round trip
  preserved every completed boarding action. Fixtures intercepted storage in
  memory and disabled cloud requests; no real save or identity writes occurred.
- NPC repair work remains tied to the current live system population. Wreck
  work and station tender work persist in world saves. Physical touch and
  physical gamepad operation have not been exercised.

- Published v0.258.0 at e87a7da543d88c6b12c3f54d06105a0e724eff41. CI
  34656222577 passed, including the actual Cloudflare deploy step. Container
  workflow 34656222168 passed. Public HTML references index-Du38lKfO.js and
  index-DLwGHPCy.css. Their live SHA-256 values match the tested build:
  JS e144a71bf5e327998aae6535e16e2f6f0887b63c79fc74894b0a71e1b94907f2;
  CSS 24cedb831f94187ee7d3f4b1266b1d7e5d61324f770628c19e261646023bb73f.
  The live health API returned ok and a fresh browser tab displayed v0.258.0.
  The final production preview reported no browser warnings or errors.
- Dev5199 was restarted to refresh its embedded version. Temporary browser
  fixtures and the production preview on 5198 were closed after verification.
  The separate Bigbox container and BoxPilot catalog PR were not changed.

## v0.259.0: Wreck salvage

- Added a boarding and salvage panel reached with E near a wreck. Four
  exterior sections supply spare parts, refined metals and engineering
  materials. Seeded stock is finite and survives visits and save loading.
- Cutting spends 0.5 fuel per unit over four seconds, or 2.8 seconds with
  an available engineer. Pausing or leaving retains paid progress. Cargo
  and material capacity stop the cutter before any fuel or stock is lost.
- Survivor rescue gates cutting. Salvage claims divide or surrender exterior
  stock with the crates. Arks cannot be stripped. Cleared legacy wrecks do
  not gain new stock. Story checks keep their interior completion semantics.
- Maps and radar retain cleared interiors with remaining exterior stock.
  Boarding returns to salvage, and salvage resumes the active flight.
- Validation: 510 tests pass, including 16 new salvage cases. TypeScript and
  production build pass. Existing Node storage and Vite size notices remain.
- Native browser checks: mouse cutting recovered two parts for one fuel and
  stopped at a two unit hold. Electronics still used material storage. A cut
  paused at 60.8 percent by leaving, then resumed and finished on return.
  All four ship contacts present before leaving the panel remained afterward.
  No browser errors or warnings were reported. Fixture writes stayed in memory.
- The production build blocked cutting while a survivor remained, accepted
  evacuation through the boarding E control at a powered cryo pod, and
  returned to the salvage panel with power and rescue progress retained.
